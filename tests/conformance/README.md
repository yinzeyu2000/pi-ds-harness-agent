# Conformance suite

Cross-implementation contracts live here: Journal, ModelGateway, ExecutionBroker, ProcessManager, SandboxProvider, ApprovalManager, PluginHost, and protocol transports. Each implementation must run the same black-box cases, including cancellation and terminal settlement.
