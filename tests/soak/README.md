# Soak suite

Long-running tests track handles, child processes, memory, Journal/projection lag, reconnect churn, plugin load/unload cycles, cancellation latency, queue fairness, and terminal-state completeness. Industrial v1 thresholds will be frozen from measured M0/M2 baselines.
