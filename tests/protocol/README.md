# Protocol suite

Protocol tests will include version fixtures, lossless JSON/schema checks, command dedupe, payload-hash conflicts, controller epochs, sequence resume, slow-consumer backpressure, reconnects, and compatibility transcripts for CLI, TUI, ACP, and future UIs.
