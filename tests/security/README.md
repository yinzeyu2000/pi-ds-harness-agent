# Security suite

Negative tests cover path traversal, symlink/reparse and TOCTOU escapes, environment leakage, command/argument confusion, network denial, approval replay/substitution, stale epochs, plugin authority escalation, sandbox downgrade, process-tree escape, and output/resource exhaustion.
